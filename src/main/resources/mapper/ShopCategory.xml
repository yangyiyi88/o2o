<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.o2o.dao.ShopCategoryDao">
    <select id="queryShopCategory" resultType="ShopCategory">
        select
        shop_category_id,shop_category_name,shop_category_desc,shop_category_img,priority,create_time,last_edit_time
        from tb_shop_category
        <where>
            <if test="shopCategory != null">
                and parent_id is not null
            </if>
            <if test="shopCategory.parent != null">
                and parent_id = #{shopCategory.parent.shopCategoryId}
            </if>
        </where>
        order by priority desc
    </select>
</mapper>